# OS
.DS_Store
Thumbs.db

# Changelog (not project code)
CHANGELOG.md

# Python
__pycache__/
*.pyc
*.pyo
venv/
*.egg-info/

# Claude Code local config (skills, agents, memory — not project code)
.agents/
.claude/
CLAUDE.md

# IDE / Editor
.obsidian/
.vscode/

# History (archived design iterations, old Django attempts)
history/

# Environment variables (API keys, passwords)
.env

# Design documents (project planning, not code)
design-*-user-file-modules.md
design-*-file-management.md
design-requirements-*.md
implementation-plan-*.md

# Server
*.pid

# Django
*.sqlite3
*.sqlite
staticfiles/

# Production settings (contains server IP/domain)
bnusparks/settings_prod.py

# Data
data/materials/
data/*.sqlite3

# Source archives (batch import sources, not repo content)
data/资料的压缩包们/

# 培养方案 JSON 存档（11MB，按需通过 import_pyfa 命令导入）
data/major_course_map.json

# 种子 PDF 临时目录（培养方案导入用，不提交）
tmp_seed_pdfs/

# Documentation (project planning, worklogs — not code)
docs/

# Deployment scripts (contain SSH credentials — NEVER COMMIT)
deploy.sh
deploy.sh.template
setup-new-server.sh
scripts/deploy.sh
scripts/deploy.py
DEPLOY.md

# One-time helper scripts / 自动化脚本（不用于生产环境，含内部信息）
scripts/

# Backup (database dump, .env — local only)
backup/

# Test artifacts
test_data/

# Django dead code (old template era, SPA 已废弃)
materials/templates/
materials/static/

# One-time import tools（不用于生产）
materials/management/

# 空测试模板（不用于生产）
materials/tests.py

# 项目元信息（不用于运行网站）

