# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo

# Claude Code local config (skills, agents, project instructions — not project code)
.agents/
CLAUDE.md

# IDE / Editor
.obsidian/

# History (archived design iterations, old Django attempts, reference materials)
history/

# Python (root-level; subdir ones are auto-ignored by git)
__pycache__/

# Server
*.pid

# Django
*.sqlite3
*.sqlite
__pycache__/
staticfiles/

# Data — 资料存储
#   data/materials/   MEDIA_ROOT — 所有资料文件（按课程代码组织，git 跟踪）
#   data/*.sqlite3    SQLite 数据库（不上传）
data/*.sqlite3

# Source archives (batch import sources, not repo content)
data/资料的压缩包们/
