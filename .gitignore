# ══════════════════════════════════════════════════════════════
# BNU Sparks · .gitignore（2026-09 仓库边界清理版）
# 原则：仓库只保留「运行 + 测试配套 + 无密钥部署模板 + 治理」；
#       宣传素材 / 原型 / 校区数据 / 凭证 / 一次性脚本只在本地。
# ══════════════════════════════════════════════════════════════

# ── OS ──
.DS_Store
Thumbs.db

# ── Python ──
__pycache__/
*.pyc
*.pyo
venv/
*.egg-info/

# ── 密钥与环境（绝不入库）──
.env
bnusparks/settings_prod_local.py
backup/

# ── 数据（用户上传、数据库、缓存、日志、校区课程目录）──
data/
*.sqlite3
*.sqlite
tmp_seed_pdfs/
staticfiles/
test_data/
*账号清单*.xlsx

# ── 部署脚本（含 SSH 凭证，永不入库；无密钥模板除外）──
deploy.sh
setup-new-server.sh

# ── 本地脚本（共享仓库只收通用脚本，其余校区专属/一次性留本地）──
scripts/*
!scripts/clean_stale_data.py
!scripts/deploy_verify.sh
!scripts/security-audit.sh

# ── AI / IDE 本机配置 ──
.agents/
.claude/
.codex/
.zcode/
.vscode/
.obsidian/
CLAUDE.md
AGENTS.md

# ── 文档与历史存档（仅白名单文档入库）──
history/
CHANGELOG.md
DEPLOY.md
design-*-user-file-modules.md
design-*-file-management.md
design-requirements-*.md
implementation-plan-*.md
docs/*
!docs/OPERATIONS.md
project-map.md
ANNOUNCEMENT_*.md
demo.html
artifacts/

# ── 服务器运行产物 ──
*.pid

# ── 测试（测试套件在私人仓库 bnusparks-tests；克隆到 materials/tests/ 使用）──
materials/tests.py
materials/tests/

# ── Django 旧模板时代遗留（SPA 已废弃）──
materials/templates/
materials/static/

# ── 部署配置目录（只收 *.conf.example 模板）──
deploy/*
!deploy/nginx/
deploy/nginx/*
!deploy/nginx/bnusparks.conf.example

# ── 工具产物 ──
output/
