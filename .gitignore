# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo

# Claude Code local config (skills, agents, memory, project instructions — not project code)
.agents/
.claude/
CLAUDE.md

# IDE / Editor
.obsidian/
.vscode/

# History (archived design iterations, old Django attempts, reference materials)
history/

# Environment variables (API keys, passwords)
.env

# Design documents (project planning, not code)
design-*-user-file-modules.md
design-*-file-management.md
design-requirements-*.md
implementation-plan-*.md

# Python
__pycache__/
*.pyc
*.pyo
venv/
*.egg-info/

# Server
*.pid

# Django
*.sqlite3
*.sqlite
staticfiles/

# Data — 资料存储
#   data/materials/   MEDIA_ROOT — 所有资料文件（按课程代码组织，git 跟踪）
#   data/*.sqlite3    SQLite 数据库（不上传）
data/*.sqlite3

# Source archives (batch import sources, not repo content)
data/资料的压缩包们/

# 培养方案 JSON 存档（11MB，按需通过 import_pyfa 命令导入）
data/major_course_map.json
docs/worklog-*.md
docs/SESSION_HANDOFF.md

# Deployment script (contains SSH credentials — DO NOT COMMIT)
deploy.sh

# Test artifacts (upload testing files)
test_data/
