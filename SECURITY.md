# 🔒 安全政策（Security Policy）

> BNU Sparks 承载真实学生数据（学号、昵称、上传资料），安全是我们的底线。

## 报告漏洞（Reporting a Vulnerability）

如果你发现安全漏洞，**请不要创建公开 Issue 或 PR**——那会让漏洞在修复前就被攻击者利用。

请通过 GitHub 的**私有漏洞披露**功能提交：

1. 打开仓库主页 → **Security** 标签 → **Report a vulnerability**
2. 描述内容：
   - 影响端点 / 页面
   - 复现步骤（含请求/参数）
   - 预期行为 vs 实际行为
   - 影响评估（是否可越权 / 窃取数据 / 执行代码）

或发送邮件至 **bnusparks@163.com**（标题注明 `[SECURITY]`）。

## 我们的承诺

- 收到报告后 **7 天内**确认并给出初步评估
- 修复后公开致谢报告者（如允许）
- 高风险漏洞优先处理

## 安全边界

本仓库为**去敏开源**（MIT），公开内容遵循以下原则：

- **不含**生产密钥、`settings_prod.py`、`.env`、SSH 凭据、TLS 私钥、服务器 IP
- **不含**测试套件与服务器架构配置——它们包含权限链/攻击面细节，仅本地与服务器保留
- **不含**任何真实用户数据与上传资料

在生产环境部署时，上述敏感项一律通过 `.env` / 服务器文件提供，请勿提交到仓库。
